# Tangerine
Terminal Game Engine in C99

All functions and structs are prefixed with `tge_`

Everything is an "object." Which is just a struct with x, y, color, and "sprite," which is just a character.

Groups are groups of objects.
